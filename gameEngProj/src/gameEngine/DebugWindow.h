#pragma once
#include"Core.h"
#include"libs.h"
#include<imgui.h>
#include<imgui_impl_glfw.h>
#include<imgui_impl_opengl3.h>

namespace gameEngine {
	/*class FUN_API debugWindow {
	protected:
		std::vector<void*> varPointer;
	public:
		std::string menu_name;
		debugWindow(std::string name) {
			menu_name = name;
		}
		inline int addToPointer(void* som) {
			int index = varPointer.size();
			varPointer.push_back(som);
			return index;
		}
		virtual void drawImGUI() {

		}

	};*/
}